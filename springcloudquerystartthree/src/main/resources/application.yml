server:
  port: 8702 #服务消费方
#指定当前eureka客户消费方
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8700/eureka,http://localhost:8698/eureka,http://localhost:8697/eureka
#  instance:
#    hostname: localhost
#当前服务名称
spring:
  application:
    name: eureka-consumer